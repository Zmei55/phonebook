export const theme = Object.freeze({
  colors: {
    activeButton: '#89a0fc',
    activeBorder: '#0d3dff',
  },
  borderForm: '1px solid #111',
  borderRadius: '4px',
  spacing: value => `${4 * value}px`,
});
